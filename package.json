{
  "dependencies": {
    "bcrypt": "^5.1.1",
    "concurrently": "^8.2.2",
    "express-session": "^1.17.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "cd server && nodemon server.js",
    "frontend": "cd client && npm start",
    "webApp": "concurrently \"npm run server\" \"npm run frontend\" "
  }
}
